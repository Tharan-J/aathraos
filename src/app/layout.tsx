import type { Metadata } from "next";
import { Inter, JetBrains_Mono } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  weight: ["300", "400", "500", "600", "700", "800", "900"],
});

const jetbrainsMono = JetBrains_Mono({
  variable: "--font-jetbrains",
  subsets: ["latin"],
  weight: ["400", "500", "600"],
});

export const metadata: Metadata = {
  title: "Aathra OS — The Operating System for Future Cities",
  description:
    "Aathra OS is a sentient campus operating system that orchestrates mobility, safety, utilities, and behavioral intelligence for smart cities using AI-driven real-time infrastructure management.",
  keywords: [
    "smart city",
    "urban OS",
    "mobility intelligence",
    "campus management",
    "AI infrastructure",
    "digital twin",
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body
        className={`${inter.variable} ${jetbrainsMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
